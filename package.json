{
  "name": "grotto-web",
  "private": true,
  "workspaces": [
    "client-web",
    "examples/basic/client"
  ],
  "scripts": {
    "build": "yarn workspace @grotto-web/client-web build",
    "test": "yarn workspace @grotto-web/client-web test",
    "example": "yarn workspace @grotto-web/example-basic-client start",
    "publish:client-web": "yarn workspace @grotto-web/client-web npm publish --access public"
  }
}
